# Provider keys are optional and should only be used by provider adapters.
# Never commit real secrets.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional: fixed verifier material secret for reproducible experiments.
# If unset, a random secret is generated per experiment. Never commit real secrets.
VICA_VERIFIER_SECRET=
